@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>ASP.NET</h1>
    <div>
        There are @Enumerable.Count(Model) tasks
    </div>
</div>

<ul>
    @foreach (var task in Model)
    {
        <li>@task.TaskName</li>
    }
</ul>
